# Руководство пользователя

Telegram-бот для отслеживания калорий и макронутриентов (КБЖУ). Бот анализирует текстовые описания и фотографии еды с помощью AI и ведёт дневник питания.

---

## Содержание

- [Первый запуск](#первый-запуск)
- [Основная клавиатура](#основная-клавиатура)
- [Логирование еды](#логирование-еды)
- [Результат анализа](#результат-анализа)
- [Редактирование записи](#редактирование-записи)
- [Удаление записи](#удаление-записи)
- [Статистика](#статистика)
- [Цели](#цели)
- [История](#история)
- [Часовой пояс](#часовой-пояс)
- [Язык интерфейса](#язык-интерфейса)
- [Напоминания](#напоминания)
- [Список команд](#список-команд)
- [Админ-команды](#админ-команды)
- [Функции в разработке](#функции-в-разработке)
- [Ограничения](#ограничения)

---

## Первый запуск

1. Отправьте боту команду `/start`.
2. Бот предложит выбрать часовой пояс. Доступно два способа:
   - **По городу** — 16 популярных городов:
     - London, Berlin, Prague, Helsinki, Moscow, Dubai, Almaty, Kolkata, Bangkok, Shanghai, Tokyo, Sydney, New York, Chicago, Denver, Los Angeles
   - **По UTC-смещению** — от UTC-12 до UTC+14
3. После выбора часового пояса появится основная клавиатура, и бот готов к работе.

Часовой пояс необходим для корректной группировки приёмов пищи по дням. Без установленного часового пояса бот не принимает ввод еды.

---

## Основная клавиатура

После настройки часового пояса в нижней части экрана отображается постоянная клавиатура с 5 кнопками:

| Кнопка | Действие |
|--------|----------|
| **📊 Stats** | Показать статистику питания |
| **🎯 Goals** | Выбрать цель (поддержание / дефицит / набор) |
| **☁️ Help** | Справка и смена часового пояса |
| **🕘 History** | Последние 20 записей |
| **✏️ Add Meal** | Начать добавление еды (приглашение к вводу) |

---

## Логирование еды

Существует три способа добавить приём пищи:

### Текстовое описание

Просто отправьте текст с описанием еды. Примеры:

- `Овсянка с бананом и мёдом`
- `200г куриной грудки, рис 150г, салат`
- `Капучино с молоком 300мл`
- `Борщ 350мл, хлеб 2 куска`

Если вы укажете точные значения (граммы, ккал, макросы), бот примет их как есть и не будет пересчитывать.

### Фотография

Отправьте фото еды. Можно добавить подпись к фото для уточнения (например, размер порции). Бот использует OpenAI Vision для распознавания блюда.

### Команда /add

Отправьте `/add` — бот попросит описать еду текстом.

---

## Результат анализа

После анализа бот отправляет карточку с информацией:

- **Название блюда** — определено AI
- **Калории** — в ккал
- **Кофеин** — отображается, если обнаружен (мг)
- **Макронутриенты**:
  - Белки (г)
  - Углеводы (г)
  - Жиры (г)
- **Ингредиенты** — список с граммами и калориями каждого
- **Статистика за сегодня** — обновлённая сумма за день

Под карточкой сохранённой записи отображаются две inline-кнопки:

| Кнопка | Действие |
|--------|----------|
| **✏️ Edit** | Редактировать запись |
| **🛑 Delete** | Удалить запись |

---

## Редактирование записи

Редактирование доступно в течение **48 часов** после сохранения.

1. Нажмите кнопку **✏️ Edit** под записью.
2. Бот спросит: «Что не так?» и предложит три варианта:
   - Отправить текст с уточнением (например, `там было 300г, а не 200г`)
   - Нажать **✅ OK** — если всё в порядке, отменить редактирование
   - Нажать **🛑 Delete it** — удалить запись
3. Если вы отправите текст, бот выполнит повторный анализ с учётом вашей обратной связи и обновит запись.

**Таймаут**: если не ответить в течение 5 минут, режим редактирования автоматически отменяется.

---

## Удаление записи

Удаление доступно в течение **48 часов** после сохранения.

1. Нажмите кнопку **🛑 Delete** под записью.
2. Запись помечается как удалённая (мягкое удаление).
3. Бот отображает обновлённую статистику за день.

Удалённые записи исключаются из всех отчётов и статистики. Физическое удаление из базы происходит через 30 дней.

---

## Статистика

Команда `/stats` или кнопка **📊 Stats** открывает выбор периода:

### Сегодня

Суммарные показатели за текущий день:
- Калории (ккал)
- Белки (г)
- Углеводы (г)
- Жиры (г)

### Неделя (7 дней)

Показатели по каждому из последних 7 дней в формате:

```
Пн 17.06: 1850 ккал | Б/У/Ж 120/200/65
Вт 18.06: 2100 ккал | Б/У/Ж 140/230/70
...
```

### 4 недели

Средние дневные показатели за каждую из 4 недель (Пн-Вс блоки):

```
Неделя 1 (10.06-16.06)
1950 ккал | Б/У/Ж 130/210/68

Неделя 2 (03.06-09.06)
1800 ккал | Б/У/Ж 125/195/62
...
```

Средние рассчитываются делением суммы за неделю на 7 (включая дни без записей).

---

## Цели

Команда `/goals` или кнопка **🎯 Goals** позволяет выбрать цель питания:

| Цель | Описание |
|------|----------|
| **Maintenance** | Поддержание текущего веса |
| **Deficit** | Дефицит калорий для снижения веса |
| **Bulk** | Профицит калорий для набора массы |

Выбранная цель сохраняется в профиле пользователя.

---

## История

Команда `/history` или кнопка **🕘 History** показывает последние **20 записей** о приёмах пищи в хронологическом порядке (новые сверху).

Каждая запись содержит:
- Дату
- Название блюда
- Калории и макронутриенты

Под каждой записью есть кнопка **🛑 Delete** для удаления.

---

## Часовой пояс

Часовой пояс выбирается при первом запуске бота. Для смены часового пояса:

1. Отправьте `/help` или нажмите кнопку **☁️ Help**.
2. Нажмите inline-кнопку **Сменить часовой пояс**.
3. Выберите город или UTC-смещение.

Часовой пояс влияет на группировку приёмов пищи по дням. Уже сохранённые записи при смене часового пояса не пересчитываются — они привязаны к дате, определённой в момент сохранения.

---

## Язык интерфейса

Команда `/language` открывает выбор языка:

- **English** — английский
- **Русский** — русский

Язык влияет на интерфейс бота, текст ответов, названия блюд и ингредиентов от AI.

---

## Напоминания

Бот автоматически отправляет напоминание, если прошло более **6 часов** с момента последней активности. Повторные напоминания отправляются не чаще одного раза в 6 часов.

---

## Список команд

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота, онбординг |
| `/help` | Справка, смена часового пояса |
| `/add` | Добавить приём пищи |
| `/stats` | Статистика питания |
| `/goals` | Выбор цели питания |
| `/history` | История записей |
| `/language` | Смена языка интерфейса |
| `/version` | Текущая версия бота |

---

## Админ-команды

Доступны только для пользователей, чьи Telegram ID указаны в переменной окружения `ADMIN_IDS`:

| Команда | Описание |
|---------|----------|
| `/admin_ping` | Проверка работоспособности |
| `/admin_stats` | Статистика сервиса |
| `/admin_limits` | Текущие лимиты |

---

## Функции в разработке

Следующие команды зарегистрированы, но пока возвращают заглушку:

- `/feedback` — отправка обратной связи
- `/subscription` — управление подпиской

---

## Ограничения

| Параметр | Значение |
|----------|----------|
| Максимальный размер фото | 5 МБ |
| Лимит запросов | 6 в минуту на пользователя |
| Одновременных анализов | 1 на пользователя |
| Максимум калорий (проверка) | 5000 ккал |
| Максимум белка (проверка) | 500 г |
| Максимум углеводов (проверка) | 800 г |
| Максимум жиров (проверка) | 400 г |
| Максимум вес порции (проверка) | 10 000 г |
| Максимум объём (проверка) | 5 000 мл |
| Максимум кофеин (проверка) | 2 000 мг |
| Окно редактирования | 48 часов |
| Окно удаления | 48 часов |
| Таймаут редактирования | 5 минут |

Значения, превышающие лимиты проверки на реалистичность, отклоняются ботом как неправдоподобные.
